import { SearchListUserPipe } from './search-list-user.pipe';

describe('SearchListUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchListUserPipe();
    expect(pipe).toBeTruthy();
  });
});
